#!/usr/bin/env node
var app = require("../server");

var port = process.env.PORT || 3000;

app.listen(port,function(err){
    if(err){
        console.dir(err);
    }
    console.log("server is listening on port:" + port + "... Yeah");
});